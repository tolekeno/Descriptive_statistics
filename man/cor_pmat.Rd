% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-helpers.R
\name{cor_pmat}
\alias{cor_pmat}
\title{Correlation and p-value matrices}
\usage{
cor_pmat(mat, method = "pearson")
}
\arguments{
\item{mat}{A numeric matrix or data frame.}

\item{method}{One of `"pearson"`, `"spearman"`, `"kendall"`.}
}
\value{
A list with `r` (correlations) and `p` (p-values), both square
  matrices with the column names of `mat`.
}
\description{
Computes pairwise correlations with [stats::cor.test()], using complete
pairs for each cell. Pairs with fewer than three complete observations, and
tests that error (for example zero variance), are left as `NA` rather than
aborting the whole matrix.
}
\examples{
cor_pmat(as.matrix(mtcars[, c("mpg", "wt", "hp")]))
}
